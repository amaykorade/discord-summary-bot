# Root .env - Used by Discord bot and Prisma CLI (db:push, migrate, etc.)
# Web app uses web/.env - see SETUP-WEB.md

# Database (PostgreSQL - Neon or any Postgres)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Discord Bot - from Developer Portal → Bot → Reset Token
DISCORD_TOKEN=your_discord_bot_token

# OpenAI API - required for AI-powered daily summaries
OPENAI_API_KEY=sk-proj-your_openai_key
